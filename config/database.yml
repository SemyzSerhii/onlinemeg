default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000
  host: localhost
  username: username
  password: password
development:
  <<: *default
  database: score_dev

production:
  <<: *default
  database: score_prod
