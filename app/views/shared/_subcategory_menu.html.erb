<ul class="nav nav-pills nav-stacked nested_nav">
  <% categories.each do |category, children| %>
    <li class="nav-item" style="width: 100%">
      <%= link_to category.name, purchases_path(filters: { category_id: category.id}),
        class: 'dropdown-item',
        style: 'text-align: center; border-bottom: 1px gray solid; width:70%; margin-left: 15%',
        remote: true
      %>
      <%= render 'subcategory_menu', categories: children if children.present?%>
    </li>
  <%end %>
</ul>
